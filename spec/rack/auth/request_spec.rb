RSpec.describe Rack::Auth::Request do
  it "has a version number" do
    expect(Rack::Auth::Request::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
